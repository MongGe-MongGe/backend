<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.gourming.model.mapper.LikeMapper">

	<insert id="insertLike">
		INSERT IGNORE INTO likes (
			user_id,
			review_id
		)
		VALUES (
			#{userId},
			#{reviewId}
		)
	</insert>

	<delete id="deleteLike">
		DELETE FROM likes
		WHERE user_id = #{userId}
		  AND review_id = #{reviewId}
	</delete>

	<select id="existsLike" resultType="boolean">
		SELECT EXISTS (
			SELECT 1
			FROM likes
			WHERE user_id = #{userId}
			  AND review_id = #{reviewId}
		)
	</select>

	<select id="countLikesByReview" resultType="long">
		SELECT COUNT(*)
		FROM likes
		WHERE review_id = #{reviewId}
	</select>

</mapper>
